<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- External services stay off in tests so the suite is deterministic. -->
        <env name="TYPESENSE_ENABLED" value="false"/>
        <env name="EMBEDDINGS_ENABLED" value="false"/>
        <env name="CHAT_ENABLED" value="true"/>
        <env name="CHAT_PREVIEW" value="false"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>

        <!--
            The api container exports these as *real* environment variables
            (see docker-compose.yml), and Laravel's Env repository reads $_SERVER
            before $_ENV/getenv — so the <env> entries above do not win. <server>
            always sets $_SERVER, which is what actually makes the test
            environment deterministic (otherwise the suite silently queries the
            live Typesense index).
        -->
        <server name="TYPESENSE_ENABLED" value="false"/>
        <server name="EMBEDDINGS_ENABLED" value="false"/>
    </php>
</phpunit>
